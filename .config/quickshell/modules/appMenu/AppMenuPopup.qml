import QtQuick
import QtQuick.Layouts
import Quickshell
import "../../components"

PopupWindow {
    id: root

    // popupWidth: 400
    // popupHeight: 700
    // moduleName: "AppMenu"
    implicitWidth: 400
    implicitHeight: 700
    color: "#ff0000"

    Text {
        text: "App Menu"
        color: "#ffffff"
    }
}
