plugin:dynamic-cursor {
    enabled = true
    mode = rotate
    rotate {
        length = 20
        offset = 0.0
    }
    shake {
        enabled = true
        nearest = true
        threshold = 1.0
        base = 4.0
        speed = 4.0
        influence = 0.0
        limit = 0.0
        timeout = 2000
        effects = false
        ipc = false
    }
    hyprcursor {
        nearest = true
        enabled = true
        resolution = -1
        fallback = clientside
    }
}
